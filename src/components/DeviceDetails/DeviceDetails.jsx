import React from 'react';

const DeviceDetails = (props) => {
    return (
        <div>
            <h1>That Device Price : {props.price}</h1>
        </div>
    );
};

export default DeviceDetails;